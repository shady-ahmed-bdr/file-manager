import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-tabs',
  imports: [RouterLink],
  templateUrl: './tabs.html',
  styleUrl: './tabs.scss'
})
export class Tabs {

}
